spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    gateway:
      discovery:
        locator:
          enabled: true
          include-expression: "'ticket-alpha'.equals(serviceId) || 'ticket-service'.equals(serviceId)"
